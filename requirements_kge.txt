# KGE Training Requirements — DFinder HKG
# Install with:  uv pip install --system -r requirements_kge.txt
#           or:  pip install -r requirements_kge.txt

# Core KGE framework
pykeen>=1.10.2

# PyTorch — Lightning AI A100 machines have CUDA torch pre-installed.
# Only uncomment the line below if running locally without GPU:
# torch>=2.1.0

# Data
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.65.0
click>=8.0.0
